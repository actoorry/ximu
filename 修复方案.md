# ximu 进销存模块 — 错误清单修复方案

> - 方案日期：2026-08-18
> - 依据文档：`错误清单.md`（2026-08-18 审查，P0×5 / P1×7 / P2×16）
> - 方案状态：**P0 五项已全部修复（2026-08-18）**；**P1-1~P1-7 已实施（2026-08-18，实施期四处决策修订见第 0 节第三轮记录）**；**P2 十五项已实施、P2-6 待产品确认（2026-08-18，实施落点见第 4.1 节，待确认问题见第 4.2 节）**。全部待组长编译验证 + 全量单测 + 空库/存量库双路径迁移演练（V1~V9）。执行纪律：Claude 改代码，组长统一 `mvn clean compile -DskipTests` 与 git 提交。

## 0. 已确认的决策（两轮确认记录）

| 决策点 | 结论 |
| ---- | ---- |
| 修复批次 | **分批：先修 P0 五项**，评审通过后再 P1、P2 |
| P0-4 初始化路径 | **Flyway 唯一来源**；演示数据用**新增 V6 清理**（不破 V1 冻结）；`data/ximu_init.sql` 删除（执行时走三次确认流程） |
| P0-1 审核字段 | **保留业务语义**：流转时 checker 取 `OperatorContext`；create/updateHead 保留 checker（预期审核人）与 auditLevel（制单人定级别）；approve 流转不再接受 auditLevel |
| P0-2 dev 便利性 | **新增 application-dev.yml 兜底**；默认 profile 删兜底（缺失即启动失败）；网关弱密钥拒绝**对所有 profile 生效** |
| P1-4 五维身份 | 本期（P1 批次）先做**服务端归一化**，主数据表另立项 |

### 第三轮（P1 实施期修订，2026-08-18）

| 决策点 | 结论 |
| ---- | ---- |
| P1-2 数量列语义 | **修正原方案缺陷**：`settle_qty` NULL=未结算（联动回退按 qty）、`actual_qty` NULL=未盘，若按原方案一刀切 NOT NULL DEFAULT 0，会把「未结算/未盘」静默篡改为「结算为零/盘亏到零」。实施改为：`qty/book_qty` NOT NULL+非负 CHECK；`settle_qty/actual_qty` **保留 NULL**+条件 CHECK（`IS NULL OR >= 0`）；盘点审核前代码侧校验 actualQty 必填 |
| P1-6 版本现实 | 联网核实（2026-08）：Spring Boot 3.x **全线 OSS EOL**（3.5 最后补丁 3.5.16），原方案「升到受 OSS 支持的 3.3/3.4」已不成立。用户确认：升 **3.5.16** + Spring Cloud 2025.0.1 + springdoc 2.8.17 + MyBatis-Plus 3.5.12；4.x 迁移（约 83 项变更）另立项 |
| P1-7 冲突处理简化 | 复合键 `(request_id, created_by)` 落地后，「命中他人 requestId」场景**物理消失**——幂等回查带当前操作人条件，他人单据根本查不到，无需原方案的 409/ConflictException；撞唯一键后回查不到即按并发异常重抛；safe_stock 侧另区分维度重复（报 400 明确业务提示） |
| P1-5 范围勘误 | safe-stock 实际**没有 Batch 端点**（原方案「Batch 同款」系记忆偏差）；update 请求体仍为 SafeStock 实体，改为逐字段 `if != null` 部分更新（null 保持原值）；并增补 `created_by` 幂等归属与 create/update 归一化，与建单侧同一套规则 |

## 1. 批次与迁移版本规划

| 批次 | 内容 | 涉及迁移 | 验收标准 |
| ---- | ---- | -------- | -------- |
| 第一批 | P0-1 ~ P0-5 | **V6**（清理演示种子） | P0 五项全部修复 + 空库/存量库双路径演练通过 |
| 第二批 | P1-1 ~ P1-7 | **V7**（索引+明细约束）、**V8**（safe_stock 键 + request_id 复合唯一键） | P1 七项清零，可上线 |
| 第三批 | P2-1 ~ P2-16 | **V9**（updated_by / driver_phone / COLLATE 等） | 上线后一个迭代内清零 |

> **V1~V5 一字不动**（已应用库 checksum 校验约束，见 `ARCHITECTURE.md` V1 冻结纪律）。

---

## 2. 第一批：P0 详细方案

### P0-1 审核人身份由客户端任意填写

**问题回顾**：`InboundController` 的 `approve`/`check` 从请求体取 `auditLevel`/`checker` 原样落库，`OperatorContext` 中的可信登录人被绕开。

**修复内容**：

1. [InboundController.java:65-72](ximu-inventory-service/src/main/java/com/by/ximu/inventory/module/inbound/InboundController.java#L65-L72)
   - `case "approve" -> inboundService.approve(id);`（删除 auditLevel 入参传递）
   - `case "check" -> inboundService.check(id);`（删除 checker 入参传递）
   - `TRANSITION_KEYS` **保留** `"auditLevel","checker","operator"` 仅供请求形态识别（值一律忽略）——兼容旧前端请求体不至于掉进普通编辑分支；多余字段的显式拒绝留给 P2-16 统一收紧。
   - 出库/盘点/调拨三个 Controller 流转本就不取身份，无需改动（`operator` 键同样保留忽略）。
2. [InboundService.java:131-149](ximu-inventory-service/src/main/java/com/by/ximu/inventory/module/inbound/InboundService.java#L131-L149) `approve(Long id)`
   - 签名去掉 `auditLevel` 参数；不再覆盖单上 auditLevel（保留制单人建单时指定的级别）。
   - 审计日志改为记录单上最终 `inbound.getAuditLevel()`。
3. [InboundService.java:154-177](ximu-inventory-service/src/main/java/com/by/ximu/inventory/module/inbound/InboundService.java#L154-L177) `check(Long id)`
   - 签名去掉 `checker` 参数；`inbound.setChecker(OperatorContext.getOperatorName())`。
   - `getOperatorName()` 为 null 时抛 `IllegalStateException("操作人上下文缺失")`（正常过滤器链不会发生，防御性断言）。
   - 审计日志 checker 同样取 `OperatorContext.getOperatorName()`。
4. `InboundCreateRequest` / `InboundUpdateRequest` 的 `checker/auditLevel` 字段**保留不动**（语义：预期审核人 / 制单人指定的审核级别）。

**验证**：
- 单测：check 流转后单据 `checker == OperatorContext` 中的操作人名，请求体传任何值均不影响；approve 后 auditLevel 等于建单时的值。
- 手工冒烟：`PUT /api/inventory/inbound/{id}` body `{"action":"check","checker":"伪造人名"}` → 单据 checker 为真实登录人。

### P0-2 安全默认值全面 fail-open

**修复内容**：

1. **默认 profile 删除兜底值**：
   - [ximu-gateway application.yml:48,51](ximu-gateway/src/main/resources/application.yml#L48)：`app.jwt.secret: "${JWT_SECRET}"`、`app.gateway-token: "${GATEWAY_TOKEN}"`（均无默认值，缺失启动即报 placeholder 解析失败）。
   - [ximu-inventory application.yml:51](ximu-inventory-service/src/main/resources/application.yml#L51) 与 [ximu-safe-stock application.yml:45](ximu-safe-stock-service/src/main/resources/application.yml#L45)：`app.gateway-token: "${GATEWAY_TOKEN}"`（删除空串兜底，杜绝"留空即跳过校验"的静默 fail-open）。
2. **新增 `application-dev.yml`**（三个服务各一份）：
   - gateway：`app.jwt.secret` 兜底为**新的 dev 专用固定密钥**（≥32 字节，不以 `change-me` 开头，例如 `dev-only-fixed-jwt-secret-2026-not-for-prod-use`）；`app.gateway-token` 兜底 `dev-shared-gateway-token`。
   - inventory / safe-stock：`app.gateway-token` 兜底 `dev-shared-gateway-token`（与网关一致）。
   - 本地启动方式变为 `--spring.profiles.active=dev`；逃生门：`GATEWAY_TOKEN=`（空串环境变量）可临时关闭下游校验（现有 blank 判断逻辑保留）。
3. **新增 `ximu-gateway/application-prod.yml`**：对齐下游两服务——`DB` 无关，含 `JWT_SECRET`/`GATEWAY_TOKEN`/`CORS_ORIGINS` 无默认值必填。
4. **网关弱密钥启动拒绝**：新增 `GatewaySecretChecker`（`@Component` + `ApplicationRunner`，gateway 包）：
   - `secret` 为空/黑名单值（旧默认值 `change-me-please-use-a-32-byte-min-secret-key` 及一切 `change-me` 前缀）→ 抛异常终止启动；
   - `secret` UTF-8 字节数 < 32 → 拒绝启动；
   - `gateway-token` 为空 → 拒绝启动（dev profile 有兜底，不受影响）。
5. **密钥轮换**（部署事项，写入部署文档）：原默认密钥已进 git 历史，视为已泄露——生产必须生成新密钥（`openssl rand -base64 48`）；代码层无动作。

**验证**：
- 不带 profile 与环境变量启动任一服务 → 启动失败（placeholder 报错）。
- `--spring.profiles.active=dev` → 三服务联动启动成功，经网关请求带 `X-Gateway-Token`。
- `JWT_SECRET=change-me-please-use-a-32-byte-min-secret-key` → 网关启动失败（弱密钥拒绝）。
- prod profile 缺任一变量 → 启动失败。

### P0-3 删除与审批 TOCTOU 竞态

**修复内容**（四个单据 Service 同款 + 库存行）：

1. **头表改条件删除**（[InboundService.java:182-199](ximu-inventory-service/src/main/java/com/by/ximu/inventory/module/inbound/InboundService.java#L182-L199)，Outbound/InventoryCheck/Transfer 的 `deleteWithItems` 同步修改）：
   ```java
   // 现状：getById 校验状态 → 删明细 → removeById(id)（无状态条件）
   // 改为：条件删除头，影响 0 行即并发冲突，整体回滚（明细删除同事务一并回滚）
   int deleted = baseMapper.delete(new LambdaQueryWrapper<Inbound>()
           .eq(Inbound::getId, id)
           .eq(Inbound::getStatus, "CREATED"));
   if (deleted == 0) {
       throw new IllegalStateException("单据状态已变化或已被他人操作，删除失败，请刷新重试");
   }
   inboundItemMapper.delete(...); // 再删明细
   ```
   - 顺序调整：**先条件删头、再删明细**（失败尽早抛出，避免无效明细删除）。
   - 状态条件已足以阻断竞态窗口（approve/check 必改 status），不额外加 version 条件（注释说明）。
   - `getById` 仍保留（权限校验 `requireCreatorOrAdmin` + 审计日志单号 + 友好报错）；`head == null` 时直接 return（与现状一致）。
   - 行为变化说明：并发删除同一单据时，后到者由"静默成功"变为"报错刷新重试"（更准确，审计不再多记一条 DELETE）。
2. **库存行条件删除**（[InventoryStockController.java:95-110](ximu-inventory-service/src/main/java/com/by/ximu/inventory/module/stock/InventoryStockController.java#L95-L110)）：
   ```java
   boolean removed = inventoryStockService.remove(new LambdaQueryWrapper<InventoryStock>()
           .eq(InventoryStock::getId, id)
           .eq(InventoryStock::getActualQty, BigDecimal.ZERO)
           .eq(InventoryStock::getTransitQty, BigDecimal.ZERO));
   if (!removed) {
       throw new IllegalStateException("库存不为零或并发冲突，不可删除（请通过出库/调拨流转处理）");
   }
   ```
   - V4 已将两列改为 NOT NULL DEFAULT 0，`eq 0` 无 NULL 不匹配陷阱；qty=0 条件即覆盖"读取后被并发改动"的窗口（任何联动必改 qty）。

**验证**：
- 单测（真实 MySQL）：T1 读到 CREATED 后由另一事务将状态改为 APPROVED，T1 删除 → 抛异常且头/明细均未被删除。
- 回归：正常 CREATED 单据删除成功；非 CREATED 删除报状态错误（保持现状语义）。

### P0-4 初始化路径地雷

**修复内容**：

1. **新增 `V6__remove_demo_seed.sql`**（`ximu-inventory-service/src/main/resources/db/migration/`）：
   - 按固定演示单号删除：`inventory_inbound`（IN20260801001/002）+ 明细、`inventory_outbound`（OUT20260805001/002）+ 明细、`inventory_check`（CK20260810001/002）+ 明细、`inventory_transfer`（TR20260808001/002）+ 明细；
   - 删除 `inventory_safe_stock` 演示配置（V3 种子三条：电解铜 A级 / 铜管 Φ20mm / 电解铜 B级）；
   - **不删 `inventory_stock` 演示四行**：库存行可能已被业务流转污染（数量已变），按五维删会误删业务行——V6 注释写明由 DBA 按需人工清理；
   - 全部 DELETE 按精确单号/品名匹配，重复执行删 0 行，天然幂等；项目未上生产，无误删真实数据风险。
2. **新增 `db/demo/demo_seed.sql`**（`ximu-inventory-service/src/main/resources/db/demo/`，Flyway locations 只扫 `db/migration`，不会自动执行）：
   - 内容 = 被清理的演示数据副本（单据头/明细、safe_stock 三条、库存四行），供演示环境在迁移完成后手工执行：`mysql -uroot -p ximu < demo_seed.sql`。
3. **删除 `data/ximu_init.sql`**：消除双路径漂移源（P2-15 的一半）。新库初始化唯一路径 = 空库启动 inventory-service（Flyway V1~V6 自动执行），演示库再手工跑 demo_seed.sql。⚠️ 删除操作执行时按约定走**三次确认**。
4. **新增 `db/precheck/pre_v4_negative_stock_report.sql`**：存量库跑 V4 前导出 `actual_qty<0 / transit_qty<0 / NULL` 差异清单人工核对（V4 的静默归零保留不动——冻结纪律 + 已演练通过，但流程上补人工核对卡点）。
5. **配置注释与文档**：`application.yml` 的 flyway 注释段更新为"唯一初始化路径"说明；README / PRODUCTION-READINESS-PLAN 增补部署章节：空库全量路径 / 存量库 baseline 路径（V2+）/ 演示数据手工路径。

**路径收敛后验证**：
- **空库**：`CREATE DATABASE ximu` → 启动 inventory-service → V1~V6 全部成功 → 表结构完整、无演示单据/种子；
- **存量库**（已应用 V1~V5 的开发库）：启动 → 仅执行 V6 → 演示单据被清、真实数据不动；
- **演示库**：迁移后手工执行 demo_seed.sql → 演示数据回来；
- 原冲突路径（ximu_init.sql 建库跑 Flyway 撞 1060）随文件删除而消失。

### P0-5 身份过滤器 `;` 路径参数绕过

**修复内容**（两服务 [OperatorContextFilter.java:37](ximu-inventory-service/src/main/java/com/by/ximu/inventory/config/OperatorContextFilter.java#L37) 同步修改，safe-stock 同款）：

1. **路径判定改用 `request.getServletPath()`**：
   - Tomcat 的 `getServletPath()` 返回已去除路径参数（`;x`）且已解码的路径，与 DispatcherServlet 路由判定一致；`/api;x/inventory/stock` → `/api/inventory/stock` → 正确进入校验分支。
   - 加注释说明为何弃用 `getRequestURI()`（原始串含 `;` 段，`startsWith("/api/")` 可被绕过）。
2. **非白名单路径直接 404**（纵深防御，不再"非 /api/ 一律放行"）：
   - 新增可配置白名单 `app.auth.public-paths`（逗号分隔前缀），默认 `/actuator/health,/actuator/info`；
   - dev profile 追加 `/swagger-ui,/v3/api-docs`（本地文档可用）；默认与 prod profile **不含** swagger → 顺带提前收敛 P2-3 的暴露面；
   - 白名单外且非 `/api/` 前缀 → 返回 404（`{"code":404,...}`，与 401 响应体同风格）。
3. `/api/` 分支内的网关令牌校验与身份头解析逻辑**不动**。

**验证**：
- `curl http://localhost:8081/api;x/inventory/stock` → 401（修复前 200 列表）；
- `curl http://localhost:8081/api/inventory/stock` 无身份头 → 401（不回归）；
- `/actuator/health` → 200；默认 profile 下 `/v3/api-docs` → 404，dev profile 下 → 200；
- 任意未定义路径 `/foo` → 404。

---

## 3. 第二批：P1 概要方案

| 编号 | 修复方向 | 主要落点 |
| ---- | -------- | -------- |
| P1-1 | **V7 迁移**：四头表补 `(status, created_at)` 组合索引；`operation_log` 补 `(module, operation, target_id, created_at)`；`detail` 的 `LIKE '%kw%'` 全模糊查询下线（keyword 降级为操作人/单号等结构化字段匹配，避免 TEXT 全表扫） | V7 + `OperationLogService` 查询逻辑 |
| P1-2 | **V7 迁移**：五张明细表数量列（`inbound_item.qty/settle_qty`、`outbound_item.qty`、`check_item.book_qty/actual_qty`、`transfer_item.qty`）补 `NOT NULL DEFAULT 0` + 非负 CHECK；存量 NULL 先回填 0（带 precheck 清单，同 P0-4 做法） | V7 |
| P1-3 | `StockOperationService` 首次入库 insert 撞 `uk_stock_dims` 后**重查重试一次**再走更新（消除裸 500）；各 Service 明细联动前**按库存五维排序**消除交叉死锁面 | `StockOperationService` + 四个 Service 的明细循环 |
| P1-4 | **已定：服务端归一化**。ximu-common 新增 `DimsNormalizer`（trim + 全角→半角 + 大小写规则，纯函数 + 单测）；入库/出库/盘点/调拨/库存行五处写入点统一调用后再入库/匹配；主数据表另立项 | ximu-common + 各 Service 写入点 |
| P1-5 | **V8 迁移**：`inventory_safe_stock` 补 `(org_id, product_name, material)` 唯一键 + `request_id` 幂等列；`SafeStockController` update 从全量覆盖改**部分更新**（白名单 DTO + null 保持原值，Batch 同款）；safe-stock 启动期加 `inventory_safe_stock` 表存在性校验（缺失即启动失败并提示部署顺序依赖） | V8 + `SafeStockService/Controller` + `BatchService` |
| P1-6 | Spring Boot 3.2.5 升级到当前受 OSS 支持的版本线（执行时联网核实 Spring 官方支持页定 3.3/3.4 具体补丁版）；升级后全量回归（重点：内嵌 Tomcat CVE-2024-34750 消除、jjwt/MyBatis-Plus/gateway 兼容） | 根 `pom.xml` |
| P1-7 | **V8 迁移**：四头表 `uk_xxx_request_id` 单列唯一 → DROP 后 ADD `(request_id, created_by)`；各 Service 幂等回查加 `created_by` 条件（越权信息泄露消除；409 分支经第三轮决策简化取消，理由见第 0 节） | V8 + 四个 Service `create()` |

### P1 实施落点（2026-08-18，代码已改；已验收：编译+108 单测+V1~V9 双路径演练）

- **迁移**：`V6`（演示库存改全值精确匹配删除）、`V7`（索引+明细约束，含第三轮语义修正）、`V8`（safe_stock 三维键+幂等列+四头表复合幂等键）；precheck×2（`db/precheck/`）随附
- **ximu-common**：新增 `DimsNormalizer`（trim+全角转半角，null 透传，不做大小写归一——DB collation 已 ci）+ `DimsNormalizerTest`
- **StockOperationService**：三方法入口归一化；insert 撞 `uk_stock_dims` catch 后重查走乐观锁更新（MySQL 语句级回滚特性）；新增 `dimsKey` 五维排序键
- **四 Service**：明细出口统一归一化（含旧单品兼容路径）；联动循环前按 dimsKey 排序；幂等回查抽 `findByIdempotent`（requestId+当前操作人）；盘点 `check` 审核 actualQty 必填
- **OperationLogService**：keyword 降级为单号/操作人（detail LIKE 下线，API 行为变化需知会前端）
- **safe-stock**：`SafeStock` 加 `requestId/createdBy` 字段；create 幂等+归一化+维度重复 400；update 逐字段部分更新+撞维度键 400；新增 `SchemaStartupCheck`（缺表启动失败）；`SafeStockCreateRequest.orgId` @NotNull
- **两服务 OperatorContextFilter**：`@PostConstruct` 空令牌 WARN（P0 复核残留点：令牌留空=校验 fail-open 零告警）
- **根 pom**：Boot 3.5.16 / Cloud 2025.0.1 / MP 3.5.12 / springdoc 2.8.17（顺手合并了重复定义两行的 springdoc.version 笔误）
- **单测**：DimsNormalizerTest（11 例）；StockOperationServiceTest 增撞键重试×3+dimsKey×2；InboundServiceTest 增幂等归属捕获断言×1

## 4. 第三批：P2 归类概要

| 类别 | 编号 | 处理 |
| ---- | ---- | ---- |
| 代码类 | P2-1 | 两服务 `GlobalExceptionHandler` 返回 `ResponseEntity` 携带真实 HTTP 状态码 |
| | P2-2 | IAE/ISE 的 `e.getMessage()` 映射固定文案，原始明细进日志 |
| | P2-13 | 各 `CreateRequest.items` 加 `@Size(max=200)` |
| | P2-14 | `Auths.requireNotSelfOrAdmin`：`createdBy=null` 视为不可审批（仅 ADMIN 可操作） |
| | P2-16 | 流转请求校验 status 一致性 / 拒绝多余字段（衔接 P0-1 的 TRANSITION_KEYS 兼容遗留） |
| | P2-7 | 基础数据审计统一走 `recordInTx` |
| | P2-6 | `transit_qty`/`stock_age` 去留 + 调拨在途量联动：**需产品确认**后定方案 |
| 配置类 | P2-3 | prod 关闭 springdoc（P0-5 的 public-paths 已收敛大半，补配置开关） |
| | P2-4 | 网关 roles 经 `Role.valueOf` 白名单；JWT 补 iss/aud 校验；内部令牌加时效（内网 TLS 属部署层） |
| | P2-8 | 按服务拆分 DB 账号最小授权（safe-stock 仅两张表 DML） |
| | P2-10 | Hikari prod 参数重定 + 补 `keepalive-time`/`leak-detection-threshold` |
| schema 类 | P2-5 | V9：全库补 `updated_by`；`operation_log.operator` 改存用户 ID |
| | P2-12 | V9：`driver_phone` 放宽 VARCHAR(32) |
| | P2-11 | V9 起建表/改表显式 `COLLATE utf8mb4_0900_ai_ci`；存量表排序规则差异由 DBA 评估统一 |
| | P2-9 / P2-15 | `operation_log` 写入收敛到 inventory 或抽共享契约；schema 单源化（P0-4 已删 ximu_init.sql 消一源，`schema.sql` 的去留随 P2-15 一并定：标注 deprecated 或由迁移导出生成） |

### 4.1 P2 实施落点（2026-08-18，代码已改；已验收：编译+108 单测+V1~V9 双路径演练）

- **P2-1/P2-2**：两服务 `GlobalExceptionHandler` 重写为 `ResponseEntity<Result>`，映射 403/400/**409**（ISE 状态机与并发冲突）/500；body `code` 语义不变。泄露面封堵：新增 `HttpMessageConversionException`（Jackson 消息含类名）与 `DataIntegrityViolationException`（含约束名）两类 handler 返回固定文案、明细进日志；IAE/ISE 消息保留（全部 throw 点均为业务文案，属可控前提，注释已声明）。safe-stock 侧补齐 ISE handler 对齐 inventory 行为
- **P2-4**：gateway 引入 `ximu-common`（仅 Role 枚举）；roles claim 逐值过 Role 白名单（列表/逗号串两种形态，畸形值丢弃）；`app.jwt.issuer/audience` 可选配置（注入即 `requireIssuer/requireAudience`，环境变量 `JWT_ISSUER/JWT_AUDIENCE`）；网关弱密钥拒绝不变。**决策说明**：「内部令牌加时效」经评估归入部署层轮换流程（静态共享令牌无状态时效语义，改双令牌轮换属部署编排），与内网 TLS 同列为部署 checklist 项，不在代码侧实现
- **P2-14**：`Auths.requireNotSelfOrAdmin` 对 `createdBy=null` 非 ADMIN 一律 403；AuthsTest 补 2 用例
- **P2-16**：四单据 Controller 流转分支加 `requireStatusMatch`（携带 `status` 且与 action 目标状态不符 → 400）；不携带 status 仍兼容。**选择「校验一致性」而非「拒绝多余字段」**：旧前端 TRANSITION_KEYS 形态仍放行，只拦真错误
- **P2-13**：四单据 CreateRequest `items` 加 `@Size(max=200)`
- **P2-7**：Batch/Stock/SafeStock 三 Controller 写方法加 `@Transactional` + `record→recordInTx`；safe-stock `OperationLogService` 补 `recordInTx`（原只有吞异常版 record）
- **P2-3/P2-10**：两服务 prod yml 加 `springdoc.api-docs/swagger-ui.enabled: false`；Hikari prod 30→20（MySQL 151 连接容量规划）+ `keepalive-time: 300000` + `leak-detection-threshold: 60000`
- **P2-5（V9）**：四头表 + safe_stock + stock + batch 补 `updated_by`；`operation_log` 补 `operator_id`（operator 姓名列保留展示）。代码侧：8 实体加字段，两服务各加 `AuditMetaObjectHandler`（strict 填充，`updatedBy` UPDATE 时 / `operatorId` INSERT 时自动取 OperatorContext，无登录上下文不填）。**决策说明**：明细表不加操作人列——明细写操作恒与头同事务同操作人，加列纯冗余；「operator 改存 ID」落地为「加 ID 列」而非「替换姓名列」——姓名保留展示与检索，ID 作唯一审计锚点
- **P2-12（V9）**：`driver_phone` VARCHAR(11)→(32)
- **P2-11（V9）**：V9 头声明排序规则规范（新表/字符列显式 `utf8mb4_0900_ai_ci`）；存量表 CONVERT 由 DBA 窗口期评估（锁表风险）；应用侧两服务 JDBC URL 加 `connectionCollation=utf8mb4_0900_ai_ci` 钳制连接层
- **P2-8**：新增 `db/grants/service_accounts.sql`（ximu_inventory 全表+DDL / ximu_safestock 两表 DML 模板）；README 环境变量表增 JWT_ISSUER/JWT_AUDIENCE 与账号拆分说明；CLAUDE.md 生产须知同步
- **P2-9/P2-15**：safe-stock `OperationLog/OperationLogService` 与 inventory 侧互指注释（同库同表双副本契约）+ `operatorId` 两边同步落地；inventory `schema.sql` 头部标注弃用（滞后于迁移链，不再维护；文件保留未删——删除需三次确认，如确认删除另行发起）
- **P2-6**：未实施，待产品确认（见 4.2）

### 4.2 P2-6 待产品确认问题（ transit_qty / stock_age / 调拨联动）

1. **调拨单是否应联动库存**？当前实现：调拨 `complete` 仅改单据状态，**不动任何库存行**（注释写明"库位间转移，总量不变"）。若产品语义是「组织间调拨」（org A → org B），则应在 approve 时加 `transit_qty`、complete 时 A 减 B 加；若是「同组织库位间移库」，现状正确但 `transit_qty` 全链路无人维护
2. **`transit_qty` 字段去留**：代码零写入点（仅 V4 约束与盘点快照读取）。选项：(a) 随调拨联动方案一并激活；(b) 确认弃用后 V10 删列；(c) 保留占位。现状库存行删除条件依赖 `transit_qty=0`，字段弃用则该条件恒真，需同步评估
3. **`stock_age` 静态列去留**：展示已走动态计算 `stockAgeDays`（now - firstInboundAt），静态列仅作回退。选项：(a) V10 删列（回退逻辑同步下线）；(b) 保留现状（PUT 接口仍可手工维护阈值字段）

> 三个问题相互耦合（调拨联动决定 transit_qty 去留），需产品给出业务语义后一并定 V10 方案。

#### 组长技术推荐（2026-08-18，基于代码事实，待产品拍板）

**问题1 — 调拨语义：推荐按「同组织库位间移库」定案，不联动库存。**
- 代码事实：`transfer_item` 只有单 `org_id` + `target_location`（目标库位），无源库位/源组织字段；`inventory_stock` 无库位维度；调拨头只挂 `batch_no`。现行模型就是"同组织内库位间移库"的形态——总量不变，账本无需变化。
- 若产品确认为「组织间调拨」（org A → org B），则属**新功能**而非缺陷修复：需新增源/目标组织维度、库存联动（approve 加在途、complete A 减 B 加）、批次在途流转，建议单独立项排期，不在本批修复链内实现。

**问题2 — transit_qty：依赖问题1，推荐 V10 删列。**
- 代码事实：唯一写入点是新库存行初始化 0（`StockOperationService`），唯一读取是库存行删除条件 `transit_qty=0`（`InventoryStockController`），全链路无在途业务。
- 若问题1按移库定案（推荐）→ transit_qty 无激活场景，V10 删列，删除条件同步简化为 `actual_qty=0 AND version=?`。
- 若问题1改为组织间调拨 → transit_qty 激活（承载在途量），V10 方案改为"联动激活 + 字段保留"。

**问题3 — stock_age：推荐 V10 删列（含回退分支下线）。**
- 代码事实：静态列恒 0（newStock 置 0），库龄预警已全量走动态 `stockAgeDays = now - firstInboundAt`；`isWarn` 的静态列回退分支仅当 `stockAgeDays==null`（即 `firstInboundAt==null`）才触发。
- 前置核对：V10 前跑只读 precheck 确认 `inventory_stock` 无 `first_inbound_at IS NULL` 行（有则人工补录）；确认后 V10 删列，`InventoryStock.isWarn` 的回退分支与 `stockAge` 字段同步下线。
- 若产品选择保留（PUT 仍可手工维护阈值），则维持现状，不新增工作。

> **结论**：三问按推荐方案（移库不联动 + transit_qty/stock_age V10 删列）为一组联动决策；产品确认后形成 V10 迁移（删列 + precheck），一个迭代内随 P2 收官。

---

## 5. 验证与回归策略（每批通用）

1. **编译与单测**：每批改完后由组长执行 `mvn clean compile -DskipTests` + 全量单测（含本批新增用例；P0 批预计新增：P0-1 审核人来源 2 例、P0-3 竞态删除 2 例、P0-5 路径判定 3 例）。
2. **数据库演练**：空库全量迁移 / 存量库增量迁移各跑一遍（P0 批重点：V6 清理只删演示单号）。
3. **手工冒烟**：按各 P0 项的"验证"小节逐条过（伪造 checker、`;` 绕过、无环境变量启动失败、demo_seed 可恢复演示数据）。
4. **跟踪表**：每批完成后同步更新 `错误清单.md` 的修复跟踪表（状态/负责人/日期）。

## 6. 前端同步说明（P0 + P2 批 API 行为变化，需同步知会前端）

### 6.1 P0 批

| # | 变化 | 影响接口 | 前端应对 |
| - | ---- | -------- | -------- |
| 1 | 流转请求体的 `checker` 字段被忽略 | `PUT /api/inventory/inbound/{id}`（action=check） | 无需传；审核人自动取当前登录人。旧前端传了也不报错、不生效 |
| 2 | 流转请求体的 `auditLevel` 字段被忽略 | `PUT /api/inventory/inbound/{id}`（action=approve） | 审核级别以制单人建单/编辑时设定的为准；流转时改级别的交互需下线 |
| 3 | 删除并发冲突从"静默成功"变为返回业务错误 | 四单据 `DELETE /api/inventory/{module}/{id}`、库存行 `DELETE /api/inventory/stock/{id}` | 需处理失败提示（message 含"刷新重试"），多为他人并发操作场景 |
| 4 | 白名单外非 `/api/` 路径直接 404 | 全部直连路径 | swagger 文档在默认/生产环境 404（本地 dev profile 可用）；`{"code":404,"message":"资源不存在","data":null}` |
| 5 | 直连 8081/8082 调试需带令牌 | 全部 `/api/**` 直连 | `-H "X-Gateway-Token: dev-shared-gateway-token"`（dev）；或经网关 8080 访问 |
| 6 | `;` 路径参数构造的请求被 401 | `/api;x/...` 类畸形路径 | 前端正常拼接路径不受影响 |

### 6.2 P2 批（2026-08-18 实施）

| # | 变化 | 影响接口 | 前端应对 |
| - | ---- | -------- | -------- |
| 7 | **HTTP 状态码真实化**（最重要）：业务错误不再一律 200 | 全部接口 | 错误分支按 `res.status` 判断：403 无权限、400 参数/状态错误、**409** 状态机冲突或并发覆盖、500 服务器错误。body 结构 `{code, message, data}` 不变，`code=1` 语义不变——若前端只解析 body 可暂不改，但应尽早迁到 HTTP 状态码判断 |
| 8 | JSON 解析失败/唯一键冲突返回 400 固定文案 | 全部写接口 | 请求体格式错误提示"请求体格式错误…"；重复提交/越界提示"数据完整性冲突…"（原始异常不再外泄，前端无需处理技术性报错文案） |
| 9 | 流转携带 `status` 与 action 目标不符 → 400 | 四单据 `PUT`（action=approve/check/complete） | 不传 `status` 不受影响（旧前端兼容）；传了就必须与 action 匹配（如 approve→APPROVED、check→CHECKED） |
| 10 | 单据明细行数上限 200 | 四单据创建接口 | 提交前分批或提示用户；超限返回 400"明细行数不能超过 200" |
| 11 | `createdBy=null` 的历史单据非 ADMIN 审批/审核 → 403 | 四单据流转接口 | 提示"单据缺少创建人信息，仅管理员可审批/审核"，引导找管理员处理历史数据 |
| 12 | 生产环境 swagger/api-docs 关闭 | `/v3/api-docs`、`/swagger-ui/**` | 生产不可访问（P0-5 已 404，P2-3 补配置双保险）；文档仅本地 dev 可用 |

## 7. 执行注意事项

- V1~V5 迁移文件**一字不动**（checksum）；一切 DDL 走 V6+。
- 删除 `data/ximu_init.sql` 前按约定执行**三次确认**流程。
- 不跑 mvn、不 git commit（组长统一）；不引入虚拟线程/Redis/Spring Security（CLAUDE.md 禁止事项）。
- P0-1 的 `TRANSITION_KEYS` 保留旧字段名仅忽略值——旧前端兼容（P2-16 已落地为「status 一致性校验」方案：携带 status 且与 action 目标不符即 400，多余字段仍兼容放行，不再另行收紧为"拒绝多余字段"）。
