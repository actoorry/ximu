# ximu 进销存模块 — 生产就绪度错误清单

> - 审查日期：2026-08-18
> - 审查范围：ximu-gateway / ximu-inventory-service / ximu-safe-stock-service / ximu-common + `db/migration`（V1~V5）+ `data/ximu_init.sql`
> - 审查方式：静态代码逐行审查（并发 / 安全 / 事务 / 幂等 / 审计 / 部署 / 性能 七个维度）
> - **总体结论：当前不可上生产。P0 共 5 项，任意一项未修复即否决上线；P1 共 7 项，上线前必须清零；P2 共 16 项，上线后一个迭代内清零。**
> - **修复状态：P0 五项 + P1 七项 + P2 十五项已全部修复并于 2026-08-18 验收通过（`mvn clean compile` ✅；全量单测 108 例全绿 ✅；空库 V1→V9 全量 / 存量库 V6→V9 增量双路径迁移演练通过，含 V7/V8 precheck 拦截验证与行为级约束验证，演练库 ximu_drill_empty / ximu_drill_legacy / ximu_drill_null 保留备查；P1 实施期四处决策修订详见 `修复方案.md` 第 0 节第三轮记录，P2 实施落点见其第 4.1 节）。生产主库现处 V8，V9 应用属上线部署动作待组长/DBA 执行。P2-6（transit_qty/stock_age 僵尸字段 + 调拨在途量联动）待产品确认业务语义，见 `修复方案.md` 第 4.2 节。**

---

## P0 致命问题（修复前禁止上线）—— ✅ 2026-08-18 已修复并验收通过

### P0-1 审核人身份由客户端任意填写，审计链失效 ✅

- **位置**：
  - `ximu-inventory-service/.../inbound/InboundController.java:69-71`（`checker` 取自请求体）
  - `ximu-inventory-service/.../inbound/InboundService.java:166`（原样落库）
  - `approve` 的 `auditLevel` 同款（InboundController.java:66-67）
- **现象**：拥有 CHECKER 角色的人发 `{"action":"check","checker":"总经理张三"}`，单据上的审核人即被写成任意字符串。`OperatorContext` 里明明有可信登录人，代码却绕开它信任请求体。
- **影响**：审计字段沦为可编辑文本框。"这笔入库谁审的"无法给出可信答案，审核流的核心价值归零。
- **修复**：流转接口删除请求体中的 `checker/auditLevel/operator` 字段，一律改取 `OperatorContext.getOperatorName()`。

### P0-2 安全默认值全面 fail-open，忘激活 prod profile 即裸奔 ✅

- **位置**：
  - `ximu-gateway/src/main/resources/application.yml:48`（JWT 密钥默认值 `change-me-please-...` 已提交进 git）
  - `ximu-inventory-service/.../config/OperatorContextFilter.java:40`（`GATEWAY_TOKEN` 为空即跳过防直连校验，三处默认均为空串）
- **现象**：
  1. 攻击者用仓库公开默认密钥自签 `{"userId":1,"roles":["ADMIN"]}` 的 HS256 token，经网关即获全部权限；
  2. 未配置令牌时 `curl http://server:8081/api/... -H "X-User-Id: 1" -H "X-User-Roles: ADMIN"` 直连伪造管理员。
  3. prod profile 虽已改为必填（正确），但依赖人工 `--spring.profiles.active=prod`，忘记时服务照常启动、零告警。
- **影响**：两道安全闸门的"启用"都依赖运维记忆，属于静默失效。
- **修复**：默认 profile 删除 `${JWT_SECRET}` / `${GATEWAY_TOKEN}` 的兜底值（缺失即启动失败）；网关启动时拒绝已知弱密钥；注意密钥已进 git 历史，需轮换并视为已泄露。

### P0-3 删除与审批存在 TOCTOU 竞态：已扣库存的单据可被删除 ✅

- **位置**：
  - `OutboundService.java:150-167` / `InboundService.java:183-199` / `InventoryCheckService.java:169-185` / `TransferService.java:165-181`（`deleteWithItems` 同款模式）
  - `InventoryStockController.java` delete（库存行删除同款）
- **现象**：先 `getById` 读到 CREATED → 删明细 → `removeById(id)`。`removeById` 只按主键删，**不带状态条件、不带 version**。
- **竞态剧本**：T1 读出单据为 CREATED → T2 并发批准（乐观锁成功，状态变 APPROVED，库存已扣减并提交）→ T1 执行删除并提交。结果：库存已少、单据连同明细被物理删除，只剩 operation_log 孤儿记录。
- **影响**：账实永久不符，且无法还原出了什么货。
- **修复**：改为条件删除 `DELETE ... WHERE id=? AND status='CREATED'`（库存行加 `actual_qty=0 AND transit_qty=0 AND version=?`），影响行数为 0 即抛并发异常。

### P0-4 初始化路径地雷：首次生产部署必踩其一 ✅

- **位置**：
  - `data/ximu_init.sql`（合并 V1~V5 最终 schema，已含 `request_id`）vs `application.yml` 的 `baseline-on-migrate: true, baseline-version: 1`
  - `db/migration/V1__baseline_full_schema.sql:218` 附近（迁移中夹带 `DELETE FROM inbound_item ...` 与"电解铜/张三"演示种子，种子库存带 3000/900 数量）
  - `db/migration/V3__add_safe_stock.sql:25`（DELETE 会删业务方预录的同名配置）
  - `db/migration/V4__stock_qty_constraint_and_field_length.sql`（迁移内将负库存静默归零，无差异清单）
- **现象**：
  1. 用 `ximu_init.sql` 建的库跑 Flyway → 跳过 V1 后执行 V2 `ADD COLUMN request_id` → 1060 duplicate column，启动崩溃；
  2. 走 Flyway 路径 → 生产库被写入演示单据/演示库存并执行破坏性 DELETE。
- **影响**：两条初始化路径都不可用于生产；迁移脚本在替业务做账（负值归零无审计）。
- **修复**：演示数据从版本化迁移剥离（挪至 `db/demo/` 或独立 profile seed）；负值归零改为先导出差异清单人工核对；明确唯一初始化路径并写入部署文档。

### P0-5 身份过滤器可被 `;` 路径参数绕过 ✅

- **位置**：`ximu-inventory-service/.../config/OperatorContextFilter.java:37`（safe-stock 同款）
- **现象**：`getRequestURI().startsWith("/api/")` 判定受保护路径，而 Spring Boot 3.2 的 PathPatternParser 路由时忽略每段 `;` 后的路径参数。请求 `/api;x/inventory/stock`：过滤器整体跳过（X-Gateway-Token 与身份校验一同失效），DispatcherServlet 却照常路由到 Controller。
- **当前状态**：因每个端点都手调 `Auths.requireRole` 而 fail-closed 兜住（已逐一核对），但这只剩一道"靠自觉"的防线——任何新端点漏写一行鉴权即完全未认证访问。
- **修复**：改用 `getServletPath()` / `ServletRequestPathUtils` 判定；非白名单路径直接 404。

---

## P1 高危问题（上线前必须清零）—— ✅ 2026-08-18 已实施并验收通过

### P1-1 列表页高频查询零索引 ✅（V7 组合索引 + 日志 keyword 下线 detail 匹配）

- **位置**：四张头表（`inventory_inbound/outbound/check/transfer`）除单号唯一键外无二级索引；`operation_log` 零二级索引且对 TEXT 列 `detail` 做 `LIKE '%kw%'`。
- **查询形态**：`WHERE status=? ORDER BY created_at DESC` + keyword 前后置通配模糊（如 `InboundService.java:51-62`）。
- **影响**：数据量增长后每次翻页全表扫描 + filesort；审计表 append-only 高增长，查询随时间线性恶化。
- **修复**：补 `(status, created_at)` 组合索引；`operation_log` 补 `(module, operation, target_id, created_at)` 索引，detail 模糊查询改为离线/ES 或取消。

### P1-2 明细表数量可 NULL、无非负约束，审核时静默跳过 ✅（V7：qty/book_qty NOT NULL+非负 CHECK；settle/actual 保留 NULL 语义+条件 CHECK）

- **位置**：`inbound_item.qty/settle_qty`、`outbound_item.qty`、`check_item.book_qty/actual_qty`、`transfer_item.qty` 均为 `DECIMAL(18,4) DEFAULT NULL`，无 NOT NULL、无 CHECK（V4 只保护了 `inventory_stock`）。
- **现象**：`InboundService.java:171-173` 对 null qty "当 0 处理"——单据有明细、库存却静默不联动。
- **影响**：Java 侧 `@Positive` 挡得住 API，挡不住直写库与维护脚本；账实差异在数据库层零拦截。
- **修复**：新迁移统一补 `NOT NULL DEFAULT 0`（或业务合适的默认值）+ 非负 CHECK。

### P1-3 库存并发首次入库 = 裸 500；多明细单存在死锁面 ✅（联动明细按五维键排序加锁，入库/出库/盘点三处；调拨不联动库存无需排序）

- **位置**：`StockOperationService.java:52-54 / 113-115`（insert 未 catch `DuplicateKeyException`）；各 Service 明细循环按客户端传入顺序逐行 version 条件更新（持有行锁）。
- **现象**：
  1. 两并发入库单同时首次入同一五维商品 → 败方撞 `uk_stock_dims` → 事务回滚 → 用户收到"系统繁忙"，单据卡 APPROVED 需人工解释（数据本身安全，唯一索引兜底正确）；
  2. 两张多明细单并发且行序相反 → 满足 InnoDB 死锁条件。
- **修复**：insert 撞唯一键后重查重试一次再更新；明细联动前按库存行主键排序消除死锁面。

### P1-4 库存身份是五个裸字符串，无品名/组织主数据 ✅（DimsNormalizer 服务端归一化，五维写入点全覆盖；主数据管理另立项）

- **位置**：全库无品名表、无组织表；`org_id` 为悬空魔法值；五维 `product_name/material/spec/grade` 依赖每次输入完全一致。
- **现象**：前导空格、全/半角差异等会静默裂变成第二行库存行，盘点对账时才暴露。
- **已证实案例**：V1 种子库存无 material（V5 补列默认 `''`），与明细种子 `material='电解铜'` 不匹配——演示库再入一笔电解铜即裂变。
- **修复**：建立品名/规格主数据表并改为外键 ID 关联（或至少加服务端归一化 trim + 大小写规则）；`org_id` 挂主数据。

### P1-5 safe_stock 表是设计弃儿 ✅（V8 三维业务唯一键 + request_id/created_by；update 改逐字段部分更新 + DuplicateKey 转业务报错）

- **位置**：`inventory_safe_stock` 表（V3 建）+ `SafeStockController.java` update。
- **问题清单**：
  1. 无 `(org_id, product_name, material)` 唯一键，同一商品可插任意多条配置，预警计算取哪条看运气；
  2. 无 `request_id` 幂等列（其余四张头表都有，唯独漏它）；
  3. update 为**全量覆盖语义**：PUT 只传一个字段，其余九个字段被清成 null——与其他模块"null 保持原值"的部分更新语义矛盾（Batch 的 update 同款）；
  4. 该表由 inventory-service 的 Flyway 创建，safe-stock-service 先启动/单独发布时首请求即 SQL 异常，无启动期校验。
- **修复**：补唯一键与幂等列；update 改部分更新语义；启动期加表存在性校验或显式声明部署顺序依赖。

### P1-6 Spring Boot 3.2.5 已过 OSS 维护期，内嵌 Tomcat 带已知 CVE ✅（升级 Boot 3.5.16 + Cloud 2025.0.1 + springdoc 2.8.17 + MP 3.5.12）

- **位置**：根 `pom.xml:26`。
- **事实**：3.2.x 开源支持 2024-12-31 终止；内嵌 Tomcat 10.1.20 在 CVE-2024-34750（HTTP/2 DoS）影响范围内。
- **修复**：升级到受支持的 3.3/3.4 线。（jjwt 0.12.5、MyBatis-Plus 3.5.9 无已知严重问题，合格。）

### P1-7 requestId 幂等键全局唯一，跨用户串单 ✅（V8 四单据表复合唯一键 (request_id, created_by) + Java 侧复合键回查）

- **位置**：`V2__add_request_id.sql:7-10`（`uk_xxx_request_id` 单列唯一）+ 各 Service `create()` 的幂等回查逻辑。
- **现象**：用户 A 用过 `requestId="abc"` 后，用户 B 用同值创建时命中 `getOne` 回查，**直接拿到 A 的单据完整内容**。
- **影响**：幂等语义错误 + 越权信息泄露。
- **修复**：唯一键改 `(request_id, created_by)`；命中时校验归属，非本人返回 409。

---

## P2 中低危问题（上线后一个迭代内清零）—— ✅ 2026-08-18 已实施 15/16 项并验收通过（P2-6 待产品确认）

> 各项实施落点与实施期决策说明见 `修复方案.md` 第 4.1 节；P2-6 待确认问题见其第 4.2 节。

| # | 问题 | 位置 | 修复方向 | 状态 |
| --- | ---- | ---- | -------- | ---- |
| 1 | 所有错误响应 HTTP 恒 200（403/500 仅在 body code 体现），WAF/监控/缓存全部按 2xx 处理 | GlobalExceptionHandler（两服务） | 返回 `ResponseEntity` 携带真实状态码 | ✅ 403/400/409/500 映射，body 结构不变 |
| 2 | IAE/ISE 的 `e.getMessage()` 原样回客户端，泄露内部类名/约束名 | GlobalExceptionHandler:28-39 | 按异常类型映射固定文案，明细进日志 | ✅ JSON 转换/唯一键冲突固定文案；业务 IAE/ISE 保留（throw 点均业务文案） |
| 3 | Swagger UI / v3/api-docs / actuator 在 8081/8082 直连无鉴权 | OpenApiConfig（两服务） | prod 关闭 springdoc 或加访问控制 | ✅ prod yml 双开关关闭（叠加 P0-5 路径 404） |
| 4 | 网关→下游明文 HTTP + 永不过期静态共享令牌；roles 逗号拼接无白名单（角色值含逗号即注入提权面）；JWT 无 iss/aud 校验 | gateway application.yml:19-23 / JwtAuthGlobalFilter | 内网 TLS；令牌加时效；roles 过 `Role.valueOf` 白名单；补 `requireIssuer/Audience` | ✅ roles 白名单+iss/aud 可选强制；TLS/令牌时效归部署 checklist |
| 5 | 行级审计链不全：无 `updated_by`、明细表无操作人、`operation_log.operator` 存姓名不存 ID | schema 全库 | 新迁移补 `updated_by`；operator 改存用户 ID | ✅ V9 加 updated_by/operator_id + 自动填充；明细表决策不加（与头同人同事务） |
| 6 | `transit_qty`/`stock_age` 僵尸字段（代码从不维护在途量，库龄无定时刷新）；调拨 COMPLETED 不联动任何库存 | StockOperationService / TransferService | 明确字段去留；调拨在途量业务盲区需产品确认 | ⏸ 待产品确认（修复方案.md 4.2 三问） |
| 7 | 非事务版 `record()` 吞异常——基础数据（批号/安全库存/库存行）审计丢了仅一行 warn | OperationLogService.java:38-53 | 关键审计统一走 `recordInTx` | ✅ 三 Controller 写方法 @Transactional + recordInTx |
| 8 | 两服务共用同一 DB 账号（dev 默认 root/123456），safe-stock 只需两张表 DML 却持全库权限 | application.yml:22-23（两服务） | 按服务拆分账号、最小授权 | ✅ db/grants/service_accounts.sql 模板 + README/CLAUDE.md 指引 |
| 9 | 两服务各持一份 `operation_log` 实体副本靠人肉同步，inventory 出 V6 改表时 safe-stock 运行期才炸 | safestock/.../OperationLog.java | 审计表写入收敛到 inventory 或抽共享契约 | ✅ 双副本互指注释契约 + V9 operator_id 双侧同步落地 |
| 10 | Hikari prod `maximum-pool-size: 30` 比 dev 的 20 大却注释"保守值"；缺 `keepalive-time`/`leak-detection-threshold` | application-prod.yml:24-29 | 按容量规划重定参数并补防泄漏配置 | ✅ 30→20 + keepalive 300s + leak-detection 60s |
| 11 | 建表不指定 COLLATE，两条初始化路径产出不同排序规则（`utf8mb4_unicode_ci` vs 默认 `0900_ai_ci`），跨表比较可能 "Illegal mix of collations" | ximu_init.sql:12 vs Flyway 路径 | 建表显式指定 COLLATE；JDBC 加 `connectionCollation` | ✅ ximu_init.sql 已删（P0-4）+ URL connectionCollation + V9 规范；存量 DBA 评估 |
| 12 | `driver_phone VARCHAR(11)` 写死大陆手机号长度，存不下 +86/座机分机 | schema.sql:55 | 放宽至 VARCHAR(32) | ✅ V9 MODIFY COLUMN VARCHAR(32) |
| 13 | `items` 列表无 `@Size` 上限，10 万行明细的 create 会长事务逐行 insert | 各 CreateRequest | 加 `@Size(max=...)` 上限（如 200） | ✅ 四单据 CreateRequest @Size(max=200) |
| 14 | `Auths.requireNotSelfOrAdmin` 对 `createdBy=null` 的历史单据直接放行 | ximu-common/Auths.java | null 视为不可审批（仅 ADMIN 可操作） | ✅ null→403 + AuthsTest 2 用例 |
| 15 | 同一份 schema 维护三处（V1 冻结 / schema.sql / ximu_init.sql），无一致性机制且已承认漂移 | schema.sql:5-7 | 收敛为 Flyway 唯一来源，其余由脚本生成 | ✅ Flyway 唯一源；schema.sql 头部弃用标注（文件保留） |
| 16 | `isTransition` 判定下 `{"action":"approve","status":"APPROVED"}` 的 status 字段被静默忽略，易误导前端 | 各单据 Controller | 流转请求校验 status 一致性或直接拒绝多余字段 | ✅ requireStatusMatch：携带且不符→400，不携带兼容 |

---

## 已验证合格项（无需处理）

- 乐观锁拦截器注册正确（`OptimisticLockerInnerInterceptor` 在分页插件之前），实体 `@Version` 齐全，扣减走 version 条件更新，并发超卖方向已堵死。
- `doc_no_seq` 使用 `INSERT ... ON DUPLICATE KEY UPDATE + LAST_INSERT_ID()` 同连接取号，多实例安全。
- `request_id` 幂等键 + 唯一索引 + `DuplicateKeyException` 回查的处理为标准做法（归属问题见 P1-7）。
- "状态流转 + 库存联动"同事务，任一步失败整体回滚；出库前校验库存不足抛异常回滚。
- update 接口走白名单 DTO，屏蔽 id/version/createdBy/时间戳；create 接口白名单 DTO（最近已补齐）。
- 网关注入身份头前先 remove 客户端伪造头；jjwt 用法正确（拒 none 算法、强制 exp、解析异常统一 401）。
- actuator 仅暴露 health/info；prod 敏感配置无默认值、缺失即启动失败。
- 数值字段使用 DECIMAL(18,4) 而非浮点；JDBC URL 含 `serverTimezone=Asia/Shanghai` 与 `characterEncoding=utf8`。
- V5 改唯一键先 DROP 再 ADD、V4 回填 NULL 后再改 NOT NULL，迁移顺序本身正确。
- `OperatorContext` ThreadLocal 在 Filter `finally` 中清理，当前同步模型下无串号（未来引入 @Async 需重新评估）。

---

## 修复顺序建议

1. **本周内（小改动大收益）**：P0-1、P0-2、P0-3、P0-5。
2. **上线前必须**：P0-4、P1-1、P1-2、P1-6。
3. **上线后一个迭代内**：P1-3 ~ P1-7、P2 全部 16 项。
4. **部署卡点**：将"激活 prod profile + 设置 JWT_SECRET / GATEWAY_TOKEN / DB_USERNAME / DB_PASSWORD / DB_URL / CORS_ORIGINS 环境变量"做成部署 checklist 强制项。

## 修复跟踪

> 修复方案见 [修复方案.md](修复方案.md)（2026-08-18，P0 详细方案 + P1/P2 概要；批次：先 P0 → P1 → P2，迁移版本 V6~V9）。

| 编号 | 状态 | 负责人 | 修复日期 | 复核人 |
| ---- | ---- | ---- | -------- | ------ |
| P0-1 | ✅ 已修复，验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P0-2 | ✅ 已修复，验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P0-3 | ✅ 已修复，验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P0-4 | ✅ 已修复，验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P0-5 | ✅ 已修复，验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P1-1 | ✅ 已实施（V7 索引+keyword 降级去 detail LIKE），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P1-2 | ✅ 已实施（V7 约束；settle_qty/actual_qty 保留 NULL 加条件 CHECK，第三轮决策修订），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P1-3 | ✅ 已实施（撞键重试+dimsKey 排序），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P1-4 | ✅ 已实施（DimsNormalizer+五处写入点），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P1-5 | ✅ 已实施（V8+幂等+部分更新+SchemaStartupCheck），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P1-6 | ✅ 已实施（Boot 3.5.16/Cloud 2025.0.1/MP 3.5.12/springdoc 2.8.17），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P1-7 | ✅ 已实施（V8 复合幂等键+回查归属；409 分支按第三轮决策简化取消），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-1 | ✅ 已实施（ResponseEntity 真实状态码），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-2 | ✅ 已实施（JSON/完整性异常固定文案封堵），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-3 | ✅ 已实施（prod 关 springdoc 双开关），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-4 | ✅ 已实施（roles 白名单+iss/aud 可选强制），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-5 | ✅ 已实施（V9 updated_by/operator_id+自动填充），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-6 | ⏸ 待产品确认（调拨联动/transit_qty/stock_age 去留；组长技术推荐方案已附于修复方案.md 4.2） | | | |
| P2-7 | ✅ 已实施（基础数据审计 recordInTx 化），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-8 | ✅ 已实施（拆账号 DDL 模板+文档指引），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-9 | ✅ 已实施（双副本互指契约+V9 双侧同步），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-10 | ✅ 已实施（Hikari 20+keepalive+leak-detection），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-11 | ✅ 已实施（connectionCollation+V9 规范+单源），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-12 | ✅ 已实施（V9 driver_phone VARCHAR(32)），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-13 | ✅ 已实施（items @Size(max=200)），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-14 | ✅ 已实施（createdBy=null 仅 ADMIN+测试），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-15 | ✅ 已实施（Flyway 唯一源+schema.sql 弃用标注），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
| P2-16 | ✅ 已实施（requireStatusMatch 一致性校验），验收通过（2026-08-18：编译+108 单测+V1~V9 双路径演练） | | 2026-08-18 | Claude |
